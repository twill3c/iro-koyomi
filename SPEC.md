# SPEC.md — iro-koyomi(色暦)

<!-- scaffold template v1.8.0 から展開(2026-08-22)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

日本の伝統色を、**色名・出典別カラーコード・その色に結びつく俳句**の三点セットで提示し、
色名の語源(=季語)を経由して**歳時記の順に一年を色で辿る**静的アプリ。

既存の伝統色サイトと異なり、本アプリは二つの不都合な事実を隠さずに主題化する。

1. **同じ色名でも出典によって HEX が違う**(色名は言葉であって数値ではない)
2. **俳人が実際に使った色語は狭い**(460 色の大半は句に現れない。空欄は空欄のまま出す)

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | **句コーパス棚卸し**(Loop 0): 著作権基準(1967 年末までに没)を満たす俳人・俳書のうち、青空文庫に**実在する**ものを列挙し、取得可能な句数・作者数・底本数、および A 層で拾える色語の異なり数を計測して `docs/census.md` に記録する。**この実測値で v1 の色規模(460 / JIS 慣用色名規模 / A 層のみ)を決定する** | must |
| F-02 | **句抽出**: 青空文庫本文から句候補を保守的規則で抽出する。判定は**記法を落とす前の原文行**に対して行い(詞書は［＃ここから１段階小さな文字］の範囲注記で囲まれるため、先に除去すると手がかりが消える)、範囲注記の集計は**本文スパンに限定**する(凡例ヘッダが注記の用例を含み、開いた領域が閉じないため)。①短行 ②句読点・全角空白を含まない ③見出し・署名の注記を持たない を満たすもののみ採る。**旧仮名・字余りのため音数(5-7-5)は採否条件にしない**。各句は表示層(原文逐語)と分析層(記法除去済み)、および bronze 本文への文字オフセットを持つ(Q-01) | must |
| F-03 | **色票基盤**: 色名 × 出典(承認済み 2 系統)の HEX を持ち、sRGB → CIELAB 変換と出典間の **ΔE2000** を算出する。出典ごとのライセンス表記と、HEX の典拠(両出典とも「記載なし」)を保持する。片方の出典にしか無い色の ΔE は `null`(欠落を一致と混同しない) | must |
| F-04 | **三層結合**: 色と句の結合を決定論規則で A / B / C に分類する。**A 直接** = 句本文に色語そのものが現れる(オフセット付き)/ **B 語源** = 色名の由来語が現れる(色への言及ではない)/ **C 空欄** = どちらもない。C は埋めない。色ごと・全体の**充填率**を算出する | must |
| F-05 | **色語辞書**: A 層検出用の色語辞書(基本色名 + 和の色名 + 変化形)。「青」が緑を含むなど**現代の HEX と語義がずれる語には語義幅フラグ**を持たせ、その句と色票 HEX を接続せずに注記表示する | must |
| F-06 | **語源 → 季語テーブル**(curated): 色名の由来語と、それに対応する季語・季節・時候を人手で確定する。諸説ある割当は certainty 列(established / contested / approximate)を持ち、**未設定の行は読み込みで拒否**する。並び順は季節・時候から導出し手入力しない。由来語が汎用語で B 層が薄い色は certainty を下げ、報告に明示する | must |
| F-07 | **gold 生成**: `web/public/data/colors/{id}.json`(出典別 HEX・ΔE・A/B 層の句と層ラベル・底本一式)+ `index.json`(層・充填率・季節・時候・歳時記順・certainty)。id は名前順の 3 桁連番で決定論。層は証拠の有無だけで決める | must |
| F-08 | **UI 一覧(色暦)**: 一年を色で辿る帯。新年・春・夏・秋・冬 × 時候順に色を配し、層(A/B/C)で塗り分け・絞り込みできる。**C の色も空欄として表示する** | must |
| F-09 | **UI 詳細(一色)**: 出典別 HEX の横並びと ΔE 表、A 層の句(色語をハイライト)、B 層の句(「色の言及ではない」旨を明示)、底本と図書カード、コントラスト比、色覚シミュレーション | must |
| F-10 | **UI 充填率**: 第一画面に「色の何割が句に現れたか」の分布図(A / B / C の内訳、句に現れた色語の頻度順位)を置く | must |
| F-11 | **アクセシビリティ機能**: P/D/T 三型の色覚シミュレーション(Viénot らの LMS 方式・係数は拘束条件から導出)の切替表示と、紙色・墨色に対するコントラスト比(WCAG 2.x)と適合等級の表示 | must |
| F-12 | フッタ: MIT License・GitHub・操作説明・設計図・App Menu の 5 リンク | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的配信のみ(Next.js 15 `output: "export"` + trailingSlash)。実行時分析なし | build + 本番確認 |
| N-02 | 青空文庫アクセスは手動コマンドのみ・間隔 ≥0.8s。`data/bronze/` はコミットしない | AGENTS 規約 |
| N-03 | **色のみで情報を伝える UI 要素を作らない**。色名・HEX・季節・層は必ずテキストでも読める | UI 確認 + テスト |
| N-04 | 純粋コア(src/core)は DOM 非依存・クライアント API は useEffect 内のみ(HC-002)。非有限値・句 0 件の色は正常系 | eslint + テスト |
| N-05 | 採用した色出典のライセンス表記を UI に常設する。規格票(JIS Z 8102 等)の色名表は転載せず参照リンクに留める | UI 確認 |
| N-06 | 季語データ基盤は `pipeline/saijiki/` と `data/curated/saijiki/` に隔離し、色に依存する型・定数を持ち込まない(将来 saijiki-lens として切り出す) | import 検査テスト |
| N-07 | 句には底本表記と図書カード URL を必ず表示する | UI 確認 |

## 4. 品質基準

| ID | ゲート | 基準 |
|---|---|---|
| G-01 | 色空間オラクル | sRGB↔CIELAB 変換と ΔE2000 が**公開既知参照値**と一致(ΔE は Sharma らの検証データセットで許容差 1e-4)。期待値を実装から転記しない |
| G-02 | 色覚・コントラストオラクル | **コントラスト比**は WCAG 2.x の定義から期待値が一意に決まるため既知値で固定(白×黒 = 21:1、同色 = 1:1、#777777×白 ≈ 4.48:1)。**色覚シミュレーション**は出所を辿れる公開検証データが見つからないため、変換が満たすべき不変量をオラクルとする — ①無彩色不変 ②冪等 ③混同軸上の色の収束(赤と緑が近づく) ④拘束に用いた原色の不変。復元係数は定数として転記せず、「白と軸原色が動かない」という拘束から解いて求める(loop_007) |
| G-03 | 結合の決定論 | 同一入力での再結合が完全一致。A / B の分類が F-04 の規則を満たす(pytest 検査)。**解釈による結合が 0 件であること** |
| G-04 | 逐語一致 | 全句の表示層本文が bronze の該当オフセットと文字単位で一致 |
| G-05 | 著作権ゲート | 採録作者の没年が全員 **≤1967**。没年不明は不採録。manifest で機械検証 |
| G-06 | 充填率の公表 | 充填率(A / B / C の実数)をレポートと UI に出す。**充填率を上げるための規則緩和は人間の承認なしに行わない** |
| G-08 | 題名オラクル | 『五百句』は序に「五百句を選んだ」とあり **抽出数 = 500 の厳密一致**。他の四冊は『六百句』序の「句数は厳格に六百句と限ったわけではなく多少超過しているかもしれぬ」(虚子)により **題名値 ≤ 抽出数 ≤ 題名値 × 1.10** の下限ゲートとする(loop_001 で確定) |
| G-07 | UI | vitest(core ≥90/85)+ verify.mjs ゲート + Playwright 実駆動 |

## 5. Loop 0 で確定するパラメータ

### 5.1 実測結果(loop_000 / `docs/census.md`)

| 項目 | 実測 |
|---|---|
| 採録作品 | 14 件(tier1 句集 12 / tier2 散文中の自句 2)。他に needs_author 2 件・不採録 2 件 |
| 作者 | 7 名(全員 1967 年末までに没・青空の著作権フラグ「なし」) |
| 句 | **6,633 句**(F-02 の正式抽出器・G-08 校正済み。loop_001 で確定) |
| 色語の異なり | 種辞書 43 語中 **31 語**(白 195 / 紅 124 / 青 101 …鴇・萌黄・若草・碧は各 1) |

主要な句集は青空文庫に実在する: 虚子の五句集(計約 3,200 句)・子規『寒山落木 巻一』(約 2,066)・
『尾崎放哉選句集』『普羅句集』『鳴雪句集』『川端茅舎句集』。山頭火は句集の形では収録されていない
(同名の散文のみ)。**句の側は十分に足りる**。

### 5.2 確定した事項

- 色の規模: **A 層で埋まるのは数十色**という census の裏付けにより、C 空欄を主題化する設計が成立する。
  色の総数は採用出典が決まり次第確定する(出典のライセンスに従属するため)
- 採録作者・作品: `data/curated/census_candidates.tsv`(全件 loop_001 で本文確認済み — HC-012)。
  『おくのほそ道』(曾良の句を含む)と『俳人蕪村』(ほぼ蕪村の句)は**編者と句の作者が異なる**ため
  `needs_author` として保留。句ごとの作者解決を設計してから採録する
- 句抽出規則: F-02 で確定(loop_001)。音数では落とさない

### 5.2b A 層の実測(loop_003)

| 項目 | 実測 |
|---|---|
| 色語を含む句 | 6,633 中 **434 句(6.5%)** |
| 色語の異なり | **21 語**(辞書 370 語彙中)。白 161・青 94・赤 50・黒 34 に集中 |
| 充填率 | 色票 369 色のうち A 層で埋まるのは **19 色(5.1%)**。残る 350 色は空欄のまま出す |
| 語義幅 | 青・蒼・碧のヒット 99 件。色票 HEX と接続しない |
| 色票に無い色語 | 蒼 4・碧 1(俳人は使うが 2 出典の色名一覧に無い) |

### 5.2c B 層と三層の充填(loop_004)

| 層 | 色数 | 割合 |
|---|---:|---:|
| A 直接(句に色語が現れる) | 19 | 5.1% |
| B 語源(由来語が句に現れる) | 80 | 21.7% |
| C 空欄 | **270** | **73.2%** |

- 由来語を確定した色は 98(季語表 53 語)。うち句に現れたのは 83 色、B 層の句は 1,069
- 由来語が汎用語(月 478 句・梅 169 句)の色は結び付きが薄い。certainty に反映し報告に明示する

### 5.3 色出典(2026-08-22 人間承認済み)

- 採用: en.wikipedia "Traditional colors of Japan" / ja.wikipedia「日本の色の一覧」(いずれも CC BY-SA 4.0)
- JIS Z 8102 は参照リンクのみ(規格票のため転載しない)
- 継承条件により `data/colors/` は **CC BY-SA 4.0** で再配布(コードは MIT)
- 実測(loop_002): 色名 **369** 件。両出典に値がある 96 件のうち **完全一致は 2 件のみ**、
  ΔE2000 の中央値 **13.96**・最大 **47.27**(黄櫨染)。同じ色名が出典によって別の色を指している

## 6. スコープ外

- **解釈による結合**(「柿くへば」→ 柿色 のような連想)。A / B のいずれにも入れない
- 1968 年以降に没した作者の句 / 校訂本文による句の「修正」
- 句の自動解釈・自動色推定・生成 AI による色の割当
- 規格票の色名表の転載
- 実行時分析・検索インデックス・ユーザ投稿
