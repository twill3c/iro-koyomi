# TEST_SPEC.md — iro-koyomi

<!-- scaffold template v1.8.0 から展開(2026-08-22) -->

## 実行規約

- Python: `python -m pytest -q tests/`(フィクスチャ駆動・**ネットワーク不要**)。web: `node scripts/verify.mjs`
- 期待値は SPEC の定義および**公開既知参照値**から取る。実装からの転記は禁止(G-01/G-02)
- 解析解を期待する合成フィクスチャは、導出前提をテスト内の assert で検算する(HC-004)
- 青空文庫本文を要するテストは、取得済み bronze の**抜粋フィクスチャ**を tests/fixtures に固定して用いる

## ケース一覧(Python)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01/G-05 | 著作権ゲート | 没年 1967 の作者は採録、1968 は不採録、没年不明は不採録。境界値を固定 |
| T-002 | F-01 | census 集計 | ミニ索引フィクスチャで作者数・作品数・句数・色語異なり数が手計算と一致 |
| T-010 | F-02/Q-01 | ルビ・注記除去 | 青空記法(`｜…《…》`・`［＃…］`)の除去後本文が期待文字列と一致 |
| T-010b | F-02 | 範囲注記の状態追跡 | `［＃ここから…］`/`［＃ここで…］` の入れ子が行ごとの集合として追える |
| T-011 | F-02 | 句候補の保守的抽出 | 地の文・見出し・詞書(小さな文字領域)が落ち、句のみ残る。**音数では落とさない**(字余り句が残ること)。凡例ヘッダの注記**用例**で領域が開きっぱなしにならないこと(loop_001 回帰) |
| T-011b | F-02/Q-01 | 二層 | 分析層はルビを落とした逐語テキスト |
| T-012 | F-02/G-04 | オフセット保持 | 抽出句の表示層本文が bronze 原文の [start,end) と文字単位で一致 |
| T-013 | F-02/G-08 | 題名オラクル | 『五百句』= 500 の厳密一致。他四冊は題名値 ≤ 抽出数 ≤ 題名値×1.10(bronze 未取得時は skip) |
| T-020 | F-03/G-01 | sRGB → CIELAB | 既知参照値(白・黒・原色ほか)と 1e-6 一致。ガンマ展開の境界(0.04045)を含む |
| T-021 | F-03/G-01 | ΔE2000 | Sharma らの検証データ全対で許容差 1e-4。ΔE(x,x)=0 と対称性 ΔE(a,b)=ΔE(b,a) も検査 |
| T-022 | F-03 | 出典パーサ | en(1 行 2 色・lang/translit テンプレート)と ja(1 行 1 色・bgcolor セル)から色名・よみ・HEX・備考を取り出す。HEX の大小文字を正規化 |
| T-022b | F-03/N-05 | 出典間差分 | 2 出典に値がある色のみ ΔE を算出し、**片方だけの色は `None`**(欠落を 0 と混同しない)。全色に provenance「記載なし」が付く |
| T-030 | F-04/G-03 | 三層分類 | ミニ色票 × ミニ句集で A / B / C の分類が規則どおり。**色語も語源語も無い句は C のまま**(埋めない) |
| T-031 | F-04/G-03 | 結合の決定論 | 同一入力で 2 回実行した結合結果が完全一致(順序含む) |
| T-032 | F-04/G-06 | 充填率 | A / B / C の実数と比率が手計算と一致。C=0 に丸める処理が存在しないこと |
| T-040 | F-05 | 色語検出 | 「白牡丹といふといへども紅ほのか」から白・紅を検出しオフセットが正しい。**牡丹の「丹」は拾わない** |
| T-040b | F-04/F-05 | 語源語は A ではない | 「山吹」単独は検出せず、「山吹色」は検出する(B 層との分離) |
| T-040c | F-05 | 複合語マスク | 紅葉・白粉・紫陽花は色の言及として数えない。同じ句の生きた色語は残る |
| T-040d | F-05 | 最長一致 | 「浅葱色」は「浅葱」より優先して 1 語で拾う |
| T-041 | F-05 | 語義幅フラグ | 青・蒼・碧は sense_width=true・connectable=false で返る |
| T-041b | F-05 | 反復 | 同一色語が 1 句に 2 回現れれば 2 件返る(期待オフセットは手計算をコメントに残す) |
| T-050 | F-06/N-06 | 季語テーブル | 色名 → 語源語 → 季語 → 季節・時候順の解決が期待どおり。certainty 未設定の行は読み込みで拒否 |
| T-051 | N-06 | 隔離 | `pipeline/saijiki/` が色関連モジュールを import していないこと(静的検査) |
| T-060 | F-07 | gold 整合 | index の層・充填率が colors/{id}.json から再計算した値と一致。全色が index に存在 |

## ケース一覧(TS)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-100 | F-11/G-02 | 色覚シミュレーション | P/D/T 各型の変換が既知参照値と一致。灰色は不変 |
| T-101 | F-11/G-02 | コントラスト比 | WCAG 定義で白×黒 = 21:1、同色 = 1:1。既知の中間値を固定 |
| T-102 | F-08 | 色暦の並び | 新年・春・夏・秋・冬 × 時候順の期待順序。季語なしの色は末尾の別枠 |
| T-103 | F-08/N-03 | 層フィルタ | A / B / C の絞り込みが期待集合。**C 選択時に空欄の色が表示される** |
| T-104 | F-09 | ΔE 表の描画データ | 出典 2 件・3 件・1 件それぞれで期待の行数と表示文字列 |
| T-105 | N-03 | 色以外の伝達 | 層・季節・色名が color 以外の属性(テキスト)で必ず出力される |
| T-106 | F-10 | 充填率図 | 内訳が合計 100% になり、C が 0 でない限り必ず描画される |
| T-107 | F-12 | フッタ 5 リンク | href とラベル一致 |
